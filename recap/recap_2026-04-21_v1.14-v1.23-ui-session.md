# Recap sessione UI — v1.14 → v1.23
**Data:** 2026-04-21
**Branch:** main
**APK finale:** `builds/v1.23-ui-session-final/HubAgency-v1.23-ui-session-final-debug.apk`
**Build:** SUCCESS

---

## Obiettivo sessione
Pulizia e redesign completo dell'UI dell'app di rilievo AR, dal layer di scansione alla home page Capacitor. Nessuna logica funzionale rimossa — solo modifiche di visibilità e stile.

---

## Modifiche per versione

### v1.14 — ui-clean
- Rimosso overlay debug GPC (coordinate `pts`, `floor:LOCK`, `ret X=`) → `visibility = View.GONE`
- Background `guidancePill` → `Color.TRANSPARENT` (init + update dinamico)
- Shadow testi guidance rafforzata: radius 16f/14f, `Color.BLACK` opaco per leggibilità su sfondo bianco
- Aggiunto `import android.view.View` mancante

### v1.15 — ui-fullscreen
- Rimossa `statsRow` ("Pronto · Vai in un angolo · Ns") → `View.GONE`
- Rimossa `qualityRow` ("BASSA MEDIA ALTA") → `View.GONE`
- `bottomBar` background → `Color.TRANSPARENT`: camera a tutto schermo

### v1.16 — ui-round-btns
- `confirmCornerBtn` → cerchio OVAL 88dp, simbolo "✓" 36sp, magenta
- Press feedback: stroke ispessisce (3→6dp) + testo si rimpicciolisce (36→30sp)
- `cancelBtn` → cerchio OVAL 64dp, sfondo bianco, freccia "↩" nera
- `mainBtnRow` ristrutto da `LinearLayout` a `FrameLayout` per posizionamento libero
- Testi dinamici fissi: `confirmCornerBtn.text = "✓"`, `cancelBtn.text = "↩"`

### v1.17 — ui-stepper
- Stepper altezza: bottoni − e + → frecce ↓ e ↑ OVAL 60dp, centrati
- Testo verde "3,70m" tra i bottoni → `View.GONE`
- `cancelBtn` allineato al bottom di `confirmCornerBtn` (entrambi `bottomMargin = dp(58)`)

### v1.18 — ui-spacing
- `distanceLabel` (testo "2,50 m"): `translationY` da 120dp → 90dp (non si sovrappone ai bottoni)
- `cancelBtn` e `confirmCornerBtn` avvolti in `btnPairRow` (`LinearLayout`) centrato con gap fisso 15dp

### v1.19 — ui-aperture
- **Bug fix**: frecce Sposta invertite — ◀ chiamava `nudgeOpening(+0.05f)` e ▶ `-0.05f`, ora corretto
- Stepper Sposta: ◀▶ → ← → ; Larghezza/Altezza/Quota: −+ → ↓ ↑
- `addStepperRow()` rifatto: bottoni OVAL 48dp, label rimossa
- `confirmOpeningBtn` → cerchio 88dp, ✓, magenta, press feedback
- `deleteOpeningBtn` → cerchio 56dp, sfondo bianco, icona 🗑, bordo magenta
- `editBtnRow` da `LinearLayout` a `FrameLayout`: confirm centrato, delete bottom-right

### v1.20 — ui-stepper-labels
- Larghezza ↔ → frecce ← → (sinistra/destra, logicamente corretto)
- Label testuali riportate negli stepper (11sp, periwinkle, shadow nera) per distinguere le righe

### v1.21 — ui-home
- Home page Capacitor completamente rifatta con palette AR:
  - Sfondo `#03040e`, titolo cyan `#14D7FF`, label `#5064B4`
  - Solo bottone "Avvia Scansione" (round pill, magenta `#B9127D`)
- Rimossi: isSupported, requestPermissions, stopScan, getScanStatus, log OUTPUT
- `requestPermissions()` integrato automaticamente in `startScan`
- Sezione "Ultimo Rilievo" appare solo dopo scansione completata
- `FloorPlanExporter.kt`: titolo immagine "Planimetria ARCore" → "Ultimo Rilievo"

### v1.22 — ui-home-bg
- Fix: spazio bianco sotto la planimetria — `html, body` con `background: #03040e` e `min-height: 100vh`

### v1.23 — ui-session-final
- APK cumulativo di tutta la sessione (build pulita)

---

## File modificati
| File | Tipo |
|---|---|
| `capacitor-plugin/android/src/main/java/it/hubagency/spatialscan/ScanningActivity.kt` | Kotlin — UI AR |
| `capacitor-plugin/android/src/main/java/it/hubagency/spatialscan/FloorPlanExporter.kt` | Kotlin — titolo planimetria |
| `android/src/main/java/it/hubagency/spatialscan/ScanningActivity.kt` | Kotlin — copia sync |
| `capacitor-test-app/src/js/capacitor-welcome.js` | JS — home page |
| `capacitor-test-app/src/index.html` | HTML — base styles |
